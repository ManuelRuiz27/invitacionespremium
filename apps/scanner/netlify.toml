[build]
command = "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @invitaciones/scanner build"
publish = "apps/scanner/dist"
ignore = "exit 0"

[build.environment]
NODE_VERSION = "22"
PNPM_VERSION = "11.15.1"
