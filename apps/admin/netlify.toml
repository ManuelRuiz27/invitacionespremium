[build]
command = "corepack enable && pnpm install --frozen-lockfile && pnpm --filter @invitaciones/admin build"
publish = "apps/admin/dist"

[build.environment]
NODE_VERSION = "22"
PNPM_VERSION = "11.15.1"
