diff --git a/apps/api/scripts/generate-openapi.ts b/apps/api/scripts/generate-openapi.ts
index 6c54c68..4779dfa 100644
--- a/apps/api/scripts/generate-openapi.ts
+++ b/apps/api/scripts/generate-openapi.ts
@@ -9,10 +9,7 @@ async function generateOpenApi(): Promise<void> {
   try {
     await app.init();
 
-    const outputPath = resolve(
-      process.cwd(),
-      process.env.OPENAPI_OUTPUT ?? 'openapi/openapi.json'
-    );
+    const outputPath = resolve(process.cwd(), process.env.OPENAPI_OUTPUT ?? 'openapi/openapi.json');
     const document = createOpenApiDocument(app);
 
     await mkdir(dirname(outputPath), { recursive: true });
diff --git a/apps/api/src/app.module.ts b/apps/api/src/app.module.ts
index 09433f4..ef98dfb 100644
--- a/apps/api/src/app.module.ts
+++ b/apps/api/src/app.module.ts
@@ -1,8 +1,4 @@
-import {
-  type MiddlewareConsumer,
-  Module,
-  type NestModule
-} from '@nestjs/common';
+import { type MiddlewareConsumer, Module, type NestModule } from '@nestjs/common';
 import { ScheduleModule } from '@nestjs/schedule';
 import { DatabaseModule } from './common/database/database.module';
 import { RequestLoggingMiddleware } from './common/logging/request-logging.middleware';
diff --git a/apps/api/src/bootstrap/create-app.ts b/apps/api/src/bootstrap/create-app.ts
index b958736..e483004 100644
--- a/apps/api/src/bootstrap/create-app.ts
+++ b/apps/api/src/bootstrap/create-app.ts
@@ -1,7 +1,4 @@
-import {
-  ConsoleLogger,
-  type INestApplication
-} from '@nestjs/common';
+import { ConsoleLogger, type INestApplication } from '@nestjs/common';
 import { NestFactory } from '@nestjs/core';
 import { AppModule } from '../app.module';
 import { AllExceptionsFilter } from '../common/errors/all-exceptions.filter';
diff --git a/apps/api/src/common/database/prisma.service.ts b/apps/api/src/common/database/prisma.service.ts
index c27eb04..4c09d55 100644
--- a/apps/api/src/common/database/prisma.service.ts
+++ b/apps/api/src/common/database/prisma.service.ts
@@ -4,10 +4,7 @@ import { AppConfigService } from '../../config/app-config.service';
 import { PrismaClient } from '../../generated/prisma/client';
 
 @Injectable()
-export class PrismaService
-  extends PrismaClient
-  implements OnModuleInit, OnModuleDestroy
-{
+export class PrismaService extends PrismaClient implements OnModuleInit, OnModuleDestroy {
   private readonly logger = new Logger(PrismaService.name);
 
   constructor(config: AppConfigService) {
diff --git a/apps/api/src/common/database/transaction-policy.ts b/apps/api/src/common/database/transaction-policy.ts
index 73b2f6c..4512683 100644
--- a/apps/api/src/common/database/transaction-policy.ts
+++ b/apps/api/src/common/database/transaction-policy.ts
@@ -9,6 +9,5 @@ export const CRITICAL_TRANSACTION_OPTIONS = {
 export const DATABASE_INVARIANT_POLICY = {
   timestamps: 'UTC',
   identifiers: 'UUID',
-  concurrency:
-    'Use PostgreSQL constraints plus serializable transactions for critical invariants.'
+  concurrency: 'Use PostgreSQL constraints plus serializable transactions for critical invariants.'
 } as const;
diff --git a/apps/api/src/common/errors/all-exceptions.filter.ts b/apps/api/src/common/errors/all-exceptions.filter.ts
index 524ddca..1409b57 100644
--- a/apps/api/src/common/errors/all-exceptions.filter.ts
+++ b/apps/api/src/common/errors/all-exceptions.filter.ts
@@ -1,17 +1,6 @@
-import {
-  ArgumentsHost,
-  Catch,
-  HttpException,
-  HttpStatus,
-  Logger,
-  type ExceptionFilter
-} from '@nestjs/common';
+import { ArgumentsHost, Catch, HttpException, HttpStatus, Logger, type ExceptionFilter } from '@nestjs/common';
 import type { Response } from 'express';
-import {
-  type RequestWithOperationId,
-  resolveOperationId,
-  resolveRouteTemplate
-} from '../logging/request-context';
+import { type RequestWithOperationId, resolveOperationId, resolveRouteTemplate } from '../logging/request-context';
 
 interface NormalizedException {
   statusCode: number;
@@ -60,9 +49,7 @@ export class AllExceptionsFilter implements ExceptionFilter {
       message: normalized.message,
       timestamp: new Date().toISOString(),
       operationId,
-      ...(normalized.details === undefined
-        ? {}
-        : { details: normalized.details })
+      ...(normalized.details === undefined ? {} : { details: normalized.details })
     });
   }
 }
@@ -92,10 +79,7 @@ function normalizeException(exception: unknown): NormalizedException {
 
   return {
     statusCode,
-    code:
-      typeof body.code === 'string'
-        ? body.code
-        : defaultCodeForStatus(statusCode),
+    code: typeof body.code === 'string' ? body.code : defaultCodeForStatus(statusCode),
     message,
     ...(body.details === undefined ? {} : { details: body.details })
   };
diff --git a/apps/api/src/common/logging/request-context.spec.ts b/apps/api/src/common/logging/request-context.spec.ts
index 431a9dd..0c9fa60 100644
--- a/apps/api/src/common/logging/request-context.spec.ts
+++ b/apps/api/src/common/logging/request-context.spec.ts
@@ -9,8 +9,6 @@ describe('resolveOperationId', () => {
   });
 
   it('replaces an invalid correlation value with a UUID', () => {
-    expect(resolveOperationId('invalid-correlation')).toMatch(
-      /^[0-9a-f-]{36}$/i
-    );
+    expect(resolveOperationId('invalid-correlation')).toMatch(/^[0-9a-f-]{36}$/i);
   });
 });
diff --git a/apps/api/src/common/logging/request-context.ts b/apps/api/src/common/logging/request-context.ts
index d2924dc..f8dc02b 100644
--- a/apps/api/src/common/logging/request-context.ts
+++ b/apps/api/src/common/logging/request-context.ts
@@ -3,8 +3,7 @@ import type { Request } from 'express';
 
 export const OPERATION_ID_HEADER = 'x-operation-id';
 
-const UUID_PATTERN =
-  /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
+const UUID_PATTERN = /^[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i;
 
 export interface RequestWithOperationId extends Request {
   operationId?: string;
diff --git a/apps/api/src/common/logging/request-logging.middleware.ts b/apps/api/src/common/logging/request-logging.middleware.ts
index 86c154a..2dcb00f 100644
--- a/apps/api/src/common/logging/request-logging.middleware.ts
+++ b/apps/api/src/common/logging/request-logging.middleware.ts
@@ -11,11 +11,7 @@ import {
 export class RequestLoggingMiddleware implements NestMiddleware {
   private readonly logger = new Logger('HttpRequest');
 
-  use(
-    request: RequestWithOperationId,
-    response: Response,
-    next: NextFunction
-  ): void {
+  use(request: RequestWithOperationId, response: Response, next: NextFunction): void {
     const operationId = resolveOperationId(request.header(OPERATION_ID_HEADER));
     const startedAt = performance.now();
 
diff --git a/apps/api/src/config/app-config.service.ts b/apps/api/src/config/app-config.service.ts
index 2bb2d4e..3dba0f3 100644
--- a/apps/api/src/config/app-config.service.ts
+++ b/apps/api/src/config/app-config.service.ts
@@ -13,9 +13,7 @@ const LOG_LEVELS: Record<EnvironmentVariables['LOG_LEVEL'], LogLevel[]> = {
 
 @Injectable()
 export class AppConfigService {
-  constructor(
-    private readonly configService: ConfigService<EnvironmentVariables, true>
-  ) {}
+  constructor(private readonly configService: ConfigService<EnvironmentVariables, true>) {}
 
   get nodeEnv(): EnvironmentVariables['NODE_ENV'] {
     return this.configService.get('NODE_ENV', { infer: true });
diff --git a/apps/api/src/config/environment.ts b/apps/api/src/config/environment.ts
index 479d9ff..3965592 100644
--- a/apps/api/src/config/environment.ts
+++ b/apps/api/src/config/environment.ts
@@ -1,8 +1,6 @@
 import { z } from 'zod';
 
-const booleanFromEnvironment = z
-  .enum(['true', 'false'])
-  .transform((value) => value === 'true');
+const booleanFromEnvironment = z.enum(['true', 'false']).transform((value) => value === 'true');
 
 const postgresUrl = z
   .string()
@@ -23,9 +21,7 @@ export const environmentSchema = z.object({
 
 export type EnvironmentVariables = z.infer<typeof environmentSchema>;
 
-export function validateEnvironment(
-  input: Record<string, unknown>
-): EnvironmentVariables {
+export function validateEnvironment(input: Record<string, unknown>): EnvironmentVariables {
   const result = environmentSchema.safeParse(input);
 
   if (result.success) {
diff --git a/apps/api/src/health/health.controller.ts b/apps/api/src/health/health.controller.ts
index bcf92ad..27b091b 100644
--- a/apps/api/src/health/health.controller.ts
+++ b/apps/api/src/health/health.controller.ts
@@ -1,9 +1,5 @@
 import { Controller, Get } from '@nestjs/common';
-import {
-  ApiOkResponse,
-  ApiServiceUnavailableResponse,
-  ApiTags
-} from '@nestjs/swagger';
+import { ApiOkResponse, ApiServiceUnavailableResponse, ApiTags } from '@nestjs/swagger';
 import { HealthResponseDto } from './health.dto';
 import { HealthService } from './health.service';
 
diff --git a/apps/api/src/health/health.service.spec.ts b/apps/api/src/health/health.service.spec.ts
index 4bb2562..fd17398 100644
--- a/apps/api/src/health/health.service.spec.ts
+++ b/apps/api/src/health/health.service.spec.ts
@@ -24,8 +24,6 @@ describe('HealthService', () => {
     } as unknown as PrismaService;
     const service = new HealthService(prisma);
 
-    await expect(service.getHealth()).rejects.toBeInstanceOf(
-      ServiceUnavailableException
-    );
+    await expect(service.getHealth()).rejects.toBeInstanceOf(ServiceUnavailableException);
   });
 });
diff --git a/apps/api/src/health/health.service.ts b/apps/api/src/health/health.service.ts
index 21675d3..0c54c30 100644
--- a/apps/api/src/health/health.service.ts
+++ b/apps/api/src/health/health.service.ts
@@ -28,8 +28,7 @@ export class HealthService {
         },
         database: {
           status: 'up',
-          latencyMs:
-            Math.round((performance.now() - databaseStartedAt) * 100) / 100
+          latencyMs: Math.round((performance.now() - databaseStartedAt) * 100) / 100
         }
       }
     };
diff --git a/apps/api/src/openapi/openapi.ts b/apps/api/src/openapi/openapi.ts
index c13e6c0..15d60b7 100644
--- a/apps/api/src/openapi/openapi.ts
+++ b/apps/api/src/openapi/openapi.ts
@@ -1,13 +1,7 @@
 import type { INestApplication } from '@nestjs/common';
-import {
-  DocumentBuilder,
-  SwaggerModule,
-  type OpenAPIObject
-} from '@nestjs/swagger';
+import { DocumentBuilder, SwaggerModule, type OpenAPIObject } from '@nestjs/swagger';
 
-export function createOpenApiDocument(
-  app: INestApplication
-): OpenAPIObject {
+export function createOpenApiDocument(app: INestApplication): OpenAPIObject {
   const config = new DocumentBuilder()
     .setTitle('InvitacionesPremium API')
     .setDescription('REST contract for InvitacionesPremium.')
diff --git a/apps/api/test/api-foundation.integration-spec.ts b/apps/api/test/api-foundation.integration-spec.ts
index fa7cfc7..5cb2ca1 100644
--- a/apps/api/test/api-foundation.integration-spec.ts
+++ b/apps/api/test/api-foundation.integration-spec.ts
@@ -57,9 +57,7 @@ describe('API foundation', () => {
   });
 
   it('serves the OpenAPI document when explicitly enabled', async () => {
-    const response = await request(app.getHttpServer())
-      .get('/docs-json')
-      .expect(200);
+    const response = await request(app.getHttpServer()).get('/docs-json').expect(200);
 
     expect(response.body.info.title).toBe('InvitacionesPremium API');
   });
diff --git a/apps/api/tsconfig.build.json b/apps/api/tsconfig.build.json
index 901fdd4..5b13313 100644
--- a/apps/api/tsconfig.build.json
+++ b/apps/api/tsconfig.build.json
@@ -6,11 +6,5 @@
     "sourceMap": true,
     "declaration": true
   },
-  "exclude": [
-    "node_modules",
-    "dist",
-    "test",
-    "scripts",
-    "src/**/*.spec.ts"
-  ]
+  "exclude": ["node_modules", "dist", "test", "scripts", "src/**/*.spec.ts"]
 }
